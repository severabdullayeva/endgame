import React from 'react'

const StartScreen = ({onStart}) => {


    return (
       
            <button onClick={onStart}>
                Start Quiz
            </button>
        
    )
}

export default StartScreen